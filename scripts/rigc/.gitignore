# check.sh writes its JUnit XML/HTML and coverage output here, beside the tool
# rather than at the repository root. Local to this directory so the migration
# adds no entry to the repository's own .gitignore, whose "Unit test / coverage
# reports" block covers .coverage/.pytest_cache but not a named report dir.
.reports/
