# Revision 2's own Kconfig fragment (rig-variants-revisions.md V1c) --
# collected only when an instance actually selects shield: i2c_sensor@2
# (cmake/modules/dts.cmake's shield Kconfig tail); a value distinguishable from any
# rig-level fragment's own MAIN_STACK_SIZE choice, so a real build's
# .config proves THIS fragment, not some other, was collected.
CONFIG_MAIN_STACK_SIZE=2600
