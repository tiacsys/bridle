# 3-cell PWM fixture rig (2-CELL REJECT case, test_three_cell_pwm.py;
# the reject half of the nonzero-flags pair): the same nonzero PWM
# flags claim as three-cell-pwm-plain's own shield, mated against the
# board's 2-cell PWM socket instead -- must still be refused.
# board is INJECTED by the test (three_cell_pwm_fixture_board), not
# declared here (rig.yml has no board: key; see reference/rig-file.rst).
rig:
  name: three_cell_pwm_2cell_reject
