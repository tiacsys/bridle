# Synthetic fixture for the empty-rig identity law (a rig with no
# instances adds nothing to its board): a rig declaring NO instances at all,
# targeting a REAL hwmv2 board target (not a fixture board.dts) so its
# resolved zephyr.dts can be compared against that same board's plain
# `west build` -- a real-build comparison that runs in the btr-shields
# harness's resolved-corpus suite, not in this repository's own gate.
rig:
  name: empty_rig
