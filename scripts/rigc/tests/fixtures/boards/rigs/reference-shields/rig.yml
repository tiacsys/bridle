# Reference-set fixture rig (rigs test suite): mates fixture_logger once and
# fixture_button twice against reference_board.dts's three sockets, exercised
# END TO END through load -> analyze -> emit as an ACCEPT case -- a reference
# implementation nobody runs is documentation, and documentation drifts.
# board is INJECTED by the test (reference_board, matching this fixture's
# own board.dts, passed via --board-dts) -- not a real hwmv2 target, and
# not declared here (board-coordinate-s6-brief.md Sec 11 retired that
# grammar).
rig:
  name: reference_shields
