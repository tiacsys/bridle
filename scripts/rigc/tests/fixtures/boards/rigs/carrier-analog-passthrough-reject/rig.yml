# Carrier PWM/ADC pass-through fixture rig (REJECT case,
# carrier-analog-passthrough-brief.md ruling 2 + Sec 5 RULED
# require-and-check): TWO independent reject scenarios in one rig
# (neither instance pair interacts with the other), so a single run
# proves both sentences at once.
rig:
  name: carrier_analog_passthrough_reject
