# Multi-plug-carrier fixture rig (REJECT case: the right plug's own
# resolved socket offers no SPI at all -- subset exposure by absence,
# slot-qualified since the carrier is plural: the diagnostic names the
# slot as well as the socket).
rig:
  name: multiplug_carrier_sockets_reject
