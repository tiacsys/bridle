# 3-cell PWM fixture rig (UNSUPPORTED-COUNT case, three-cell-pwm-brief.md
# Sec 3a, acceptance criterion 5): the board itself
# (three_cell_pwm_unsupported_board.dts) carries the bad socket -- no
# instances needed, exactly like three-cell-pwm-mismatch.
# board is INJECTED by the test, not declared here.
rig:
  name: three_cell_pwm_unsupported
