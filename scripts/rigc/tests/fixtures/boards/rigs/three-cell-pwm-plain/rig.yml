# 3-cell PWM fixture rig (PLAIN direct-mount ACCEPT case,
# test_three_cell_pwm.py): a single shield mates the board's own
# 3-cell PWM socket directly (no carrier), authoring a nonzero PWM flags
# value -- proving a four-word pwms property is emitted and that the
# nonzero flags are carried on a 3-cell socket, together.
# board is INJECTED by the test (three_cell_pwm_fixture_board), not
# declared here (rig.yml has no board: key; see reference/rig-file.rst).
rig:
  name: three_cell_pwm_plain
