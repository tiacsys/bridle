# Synthetic fixture: no instances -- the defect this pins is scan-time and
# unconditional (every shield folder is scanned on every run), so nothing
# here needs to reference either shield at all.
instances: []
