# The shared arduino_r3 alias (Ruling 1, board-as-invocation-coordinate-
# brief.md Sec 2 -- already landed on both real boards' own devicetree)
# rather than a board-prefixed socket label, so this resolves regardless
# of which real board -DBOARD names -- see rig.yml.
instances:
  - name: logger
    shield: adafruit_data_logger
    socket: arduino_r3
