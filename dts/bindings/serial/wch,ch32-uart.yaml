description: CH32 UART node

compatible: "wch,ch32-uart"

include: uart-controller.yaml

properties:
  reg:
    required: true

  clocks:
    required: true

  irq:
    type: array
    required: true

  pinctrl:
    type: array
    required: true

  remap:
    type: array
    default: [0]
